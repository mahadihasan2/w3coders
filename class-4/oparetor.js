// operator JS

// 1. Arithmetics Operators 
(Addition, Subtraction, Multiplication, Exponentiation, Division, Modulus(Division Remainder), Increment(pre Increment, Post Increment), Decrement(pre Decrement, post decrement))
    // End of ArithMetics Operator 


    // 2. Assignment Operator
    (=, +=, *, / , % , *=, **=, / =, %=, ***= etc)
    // 3. String Operator
    (Same);
// 4. Comparison   Operator
(>, < , >=, <= , == , === , != , !== , >== , <== etc);
// 5. conditional Operator
(if, else, else -if, switch , ternary);
// 6. Logical Operator
     (&& (LogiCal and), || (Logical Or) );
// 7. BitWise Operator
(&, ~, ^,< , >);

